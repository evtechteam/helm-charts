---
apiVersion: v2
appVersion: '4.1.1'
name: node-red-gitops
version: 2.0.0
description: Node-RED Helm Chart allowing GitOps workflow pulling flow files from git repositories
icon: https://nodered.org/about/resources/media/node-red-icon-2.png
maintainers:
  - name: TallTechDude
    email: talltechdude@users.noreply.github.com
sources:
  - https://github.com/node-red/node-red
  - https://github.com/evtechteam/helm-charts
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "BREAKING: Default Node-RED image upgraded to v4.1.1 from ghcr.io registry (was v3.0.2 from Docker Hub)"
    - kind: changed
      description: "BREAKING: Smart resource naming now enabled by default - avoids duplicate names when instance matches release"
    - kind: added
      description: "Smart resource naming with automatic deduplication when instance name equals release name"
    - kind: added
      description: "Automatic name truncation at 63 characters (Kubernetes limit) with hash-based uniqueness"
    - kind: added
      description: "Legacy naming opt-out via naming.smart=false for backward compatibility"
    - kind: fixed
      description: "Resource names now respect Kubernetes DNS label length limits"
